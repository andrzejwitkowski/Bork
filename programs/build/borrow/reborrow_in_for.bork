// exit: 3
fun bump(buf: &[i32; 2]) {
    buf[0] = buf[0] + 1
}

fun outer(buf: &[i32; 2]) {
    for (i in 0..2) {
        bump(&buf)
    }
}

fun main(): i32 {
    var a: [i32; 2] = [1, 0]
    outer(&a)
    return a[0]
}
